/** @type {import('tailwindcss').Config} */
export default {
  content: ['./index.html', './src/**/*.{ts,tsx}'],
  theme: {
    extend: {
      colors: {
        cream: '#F4F1E9',
        'soft-cream': '#FEFBF3',
        navy: '#1C2A48',
        gold: '#B89968',
        // ── Дизайн-система «Маршрут» (src/components/path/*) ──────────────
        // Префикс rt-, потому что имена gold и navy уже заняты живой палитрой
        // LAURA/LOCI/онбординга — 32 файла вне PATH. Когда те разделы
        // мигрируют, префикс снимается, а старые токены удаляются
        // (см. MIGRATION-TODO.md).
        // Значения — родная палитра приложения (cream/navy/gold), чтобы раздел
        // не выпадал из остального интерфейса. Из палитры «Маршрута» оставлены
        // только два служебных оттенка золота, которых в старой не было:
        // тёмное золото для мелкого текста (у #B89968 на cream контраст 2.2:1,
        // это нечитаемо) и мягкое золото для подложек.
        rt: {
          paper: '#F4F1E9', // = cream, фон страницы
          'paper-2': '#FEFBF3', // = soft-cream, карточки и таббар
          ink: '#1C2A48', // = navy, основной текст и заголовки
          'ink-2': '#4E5A75', // текст в карточках, ≈ navy/75 на cream
          'ink-3': '#7A8398', // мета, подписи, ≈ navy/55
          gold: '#B89968', // = gold, линии, ромбы, номера
          'gold-ink': '#7E5F2A', // мелкий золотой текст (5.2:1 на paper)
          'gold-lite': '#D4B36A', // золото на тёмном
          'gold-soft': '#E2D6BA', // подложки терминов, рамки чипов
          navy: '#1C2A48', // = navy, тёмный блок, залитые ромбы
          line: '#DDD6C6', // разделители, границы, ≈ navy/12 на cream
          'on-navy': '#E7E2D5', // текст на тёмном блоке, ≈ cream/85
        },
        // Токены редизайна контентных страниц (src/components/content/*).
        // ЛЕГАСИ: заменяется системой «Маршрут», удалить после раскатки.
        content: {
          bg: '#F3EFE4',
          surface: '#FBF9F2',
          navy: '#1D2B4A',
          gold: '#A98950',
          'gold-bg': '#F0E8D6',
          ink: '#23262E',
          'ink-2': '#61646C',
          line: '#E2DCCB',
        },
      },
      fontFamily: {
        serif: ['"Playfair Display"', 'serif'],
        sans: ['Inter', 'sans-serif'],
        golos: ['"Golos Text"', '-apple-system', 'sans-serif'],
        // ── «Маршрут» ──
        // Все три роли отданы Playfair Display — фирменной антикве приложения.
        // Отдельные Unbounded / IBM Plex Mono / EB Garamond из макета сюда не
        // берём: на фоне остальных экранов они читались как другой продукт.
        // Роли различаются кеглем, курсивом и разрядкой, а не гарнитурой.
        display: ['"Playfair Display"', 'serif'], // h1 страницы
        mono: ['"Playfair Display"', 'serif'], // номера станций, мета, чипы
        gloss: ['"Playfair Display"', 'serif'], // итальянские глоссы, курсив
        // Каллиграфическая заглавная «L» Лауры (аватарка, иконка таба,
        // первая буква приветствия). Только латиница — этого достаточно.
        script: ['"Great Vibes"', 'cursive'],
      },
    },
  },
  plugins: [],
};
